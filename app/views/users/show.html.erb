<h1>User info</h1>

  <p>name<%= @user.name %></p>
  <p>introduction<%= @user.introduction %></p>
  <!--<%= image_tag @user.get_profile_image(100,100) %>-->
  <%= render 'edit' %>









<h1>Books</h1>
<tabel>
  <tr>
    <th></th>
    <th>Title</th>
    <th>Opinion</th>
  </tr>
  <% @books.each do |book| %>
    <tr>
      <td></td>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
    </tr>
  <% end %>
</tabel>